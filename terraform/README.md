# Terraform

Terraform contains reusable modules and environment entrypoints.

- `modules/network`: network and boundary abstractions
- `modules/compute`: compute grouping abstractions
- `environments/dev`: first deployable environment scaffold

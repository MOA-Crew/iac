all:
  vars:
    ansible_user: ubuntu
    timezone: Asia/Seoul
  children:
    bastion:
      hosts:
        sw-hub-bastion:
          ansible_host: 127.0.0.1
    app:
      hosts:
        sw-hub-app-1:
          ansible_host: 10.10.11.11
        sw-hub-app-2:
          ansible_host: 10.10.11.12
